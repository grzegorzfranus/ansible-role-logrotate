---
# =============================================================================
# Ansible Role: Logrotate - Common Variables
# =============================================================================
# This file defines common variables shared across all distributions.
# These values are distribution-agnostic and apply to all supported systems.
#
# Sections:
#   1. Package Settings
#   2. Logrotate Configuration Paths
#   3. Supported Platforms
# =============================================================================

# -----------------------------------------------------------------------------
# 1. Package Settings
# -----------------------------------------------------------------------------
# Package name for logrotate (consistent across Debian/Ubuntu)
# Valid values: package name string e.g. "logrotate"
logrotate_package_name: "logrotate"

# -----------------------------------------------------------------------------
# 2. Logrotate Configuration Paths
# -----------------------------------------------------------------------------
# Directory containing per-application logrotate configuration files
# Valid values: absolute directory path string starting with /
logrotate_d_directory_path: "/etc/logrotate.d"

# Path to the main logrotate configuration file
# Valid values: absolute file path string starting with /
logrotate_main_config_path: "/etc/logrotate.conf"

# -----------------------------------------------------------------------------
# 3. Supported Platforms
# -----------------------------------------------------------------------------
# List of distribution and major version combinations officially supported and
# covered by the Molecule test matrix. Format: "<Distribution>-<major_version>".
# Keep synchronized with meta/main.yml platforms, the README OS support table,
# and the molecule-distros matrix in .github/workflows/ci.yml
# Valid values: list of "<Distribution>-<major_version>" strings
logrotate_supported_platforms:
  - "Debian-12"
  - "Debian-13"
  - "Ubuntu-24"
  - "Ubuntu-26"
